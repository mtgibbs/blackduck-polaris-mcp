## Codebase Patterns

- **3-layer architecture**: MCP Tool → Service Layer → API Layer
  - Tools in src/mcp/tools/ define schemas and call services
  - Services in src/services/ contain business logic
  - API layer in src/api/ handles HTTP calls with vendor media types
- **Service imports**: Always import from services/index.ts, never from api/ directly
- **ProjectMapping structure**: Has `id`, `projectId`, `configurationId`, `btsProjectKey`, `btsIssueType` (default issue type)
- **Error patterns**: Return `{error: string}` for user-facing error messages with actionable guidance

---

## 2026-02-26T03:40:00Z - S05-1

**What was implemented:**
- Created src/mcp/export-helpers.ts with resolveExportParams() function
- Function auto-resolves project_mapping_id from config_id + project_id
- Function auto-resolves bts_issue_type_id from mapping default or explicit parameter
- Returns {projectMappingId, issueTypeId} on success
- Returns {error: string} with clear guidance when resolution fails
- Handles case where no mapping found → guides user to get_config_project_mappings or create_config_project_mapping
- Handles case where no default issue type → guides user to get_external_issue_types
- Respects explicit btsIssueTypeId parameter (skips auto-resolution)

**Files changed:**
- src/mcp/export-helpers.ts (created)
- prp.json (updated S05-1 passes: true)

**Learnings for future iterations:**
- Network access is restricted in this sandboxed environment — cannot download Deno or external dependencies
- Deno verification commands (deno task check, deno task lint, deno fmt --check) cannot be run locally
- Manual code review against existing patterns is necessary: verify imports, types, and logic flow
- getConfigProjectMappings() is already available in services layer and returns ProjectMapping[]
- ProjectMapping.btsIssueType contains the default issue type ID for the mapping
- Follow the union type pattern for success/error: `{success: data} | {error: string}`

---
## 2026-02-26T04:00:00Z - S05-2

**What was implemented:**
- Modified src/mcp/tools/export-issues.ts to support smart auto-resolution
- Added optional `project_id` parameter to schema with clear description
- Changed `project_mapping_id` from required to optional
- Added validation: either `project_mapping_id` or `project_id` must be provided
- Added validation: `bts_issue_type_id` required when creating new ticket (no `bts_key`) unless using auto-resolution
- Handler calls `resolveExportParams` when `project_mapping_id` is omitted but `project_id` is provided
- Returns `errorResponse` with actionable guidance when resolution fails
- Existing callers with explicit `project_mapping_id` continue to work unchanged (backward compatible)

**Files changed:**
- src/mcp/tools/export-issues.ts (modified - added project_id param, auto-resolution logic, validation)
- prp.json (updated S05-2 passes: true)

**Learnings for future iterations:**
- The auto-resolution pattern works well: check for missing required params, call resolution helper, handle error/success union type
- Validation should happen BEFORE auto-resolution to catch missing params early
- When adding optional params that replace required params, ensure backward compatibility by supporting both paths
- Error messages should be actionable: tell users which tool to use to get missing values
- The `!` assertion on `resolvedProjectMappingId` is safe because validation ensures it's set (either explicit or resolved)

---

## 2026-02-26T04:05:00Z - S05-3

**What was implemented:**
- Moved bts_issue_type_id validation to AFTER auto-resolution (critical fix)
  - Previous validation checked `!bts_key && !bts_issue_type_id && !project_id` which was too early
  - New validation checks `!bts_key && !resolvedBtsIssueTypeId` after resolution completes
  - This catches cases where auto-resolution runs but doesn't provide an issue type
- Updated bts_issue_type_id description to reference get_external_issue_types (was incorrectly referencing get_external_projects)
- Updated branch_id description to clarify default branch behavior: "If omitted, the project default branch is used."
- Improved error message clarity with actionable guidance:
  - "Use get_config_project_mappings to find project_mapping_id for your project."
  - "Use get_external_issue_types to find available issue types for your bug tracking system."

**Files changed:**
- src/mcp/tools/export-issues.ts (validation logic reordered, schema descriptions updated)
- prp.json (updated S05-3 passes: true)

**Learnings for future iterations:**
- **Validation timing is critical**: When you have auto-resolution logic, validate AFTER resolution, not before
  - Early validation can't check if resolution succeeded in providing required values
  - Post-resolution validation catches all failure modes (missing params + failed resolution)
- **Schema descriptions matter**: Tool parameter descriptions should reference the correct tools users need to call
  - Incorrect tool references (like get_external_projects vs get_external_issue_types) confuse LLMs and users
- **Error messages should be specific and actionable**: Instead of generic "missing parameter" errors, tell users exactly which tool to call
- **Comment your non-obvious logic**: The validation reordering needed a comment explaining WHY it happens after resolution

---

## 2026-02-26T04:40:00Z - S05-4

**What was implemented:**
- Fixed getExternalProjects() pagination limit from default 100 to explicit 50
- Added comment explaining API max limit is 50
- Verified all other getAllOffset() calls use appropriate limits (default 100 is correct for their respective APIs)
- Confirmed branch_id descriptions already correctly updated in S05-3 (export-issues.ts and export-findings-issues.ts both have proper descriptions)

**Files changed:**
- src/api/bug-tracking.ts (modified - added explicit limit parameter to getExternalProjects getAllOffset call)
- prp.json (updated S05-4 passes: true)

**Learnings for future iterations:**
- **Pagination limits vary by API endpoint**: Not all Polaris APIs have the same max limit
  - External projects API: max 50
  - Most other APIs: max 100
  - Check API documentation or context hints for specific limits
- **getAllOffset default parameter**: The client.getAllOffset() method has a default limit of 100 (4th parameter)
  - When an API has a lower max, pass an explicit limit to the getAllOffset call
  - The explicit limit should include a comment explaining why it differs from the default
- **Verification across multiple calls**: When acceptance criteria mention "verify all other calls", use grep/search to find all instances
  - Pattern: grep for the function name (getAllOffset) across relevant directories
  - Verify each call either uses an appropriate explicit limit or the default is correct
- **Branch_id descriptions**: These were already fixed in S05-3, showing that stories can have overlapping scope
  - Always check prior work in related stories before implementing
  - The progress log helps identify what's already been done

---

