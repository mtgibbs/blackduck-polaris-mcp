## Codebase Patterns

- Filter validation is advisory-only: warns but does not block unknown keys (API may add new keys)
- RSQL filter key extraction uses simple regex, not a full parser: /([a-zA-Z0-9:._-]+)\s*(?:==|!=|=in=|=out=|=like=)/g
- Portfolio tools use bare filter keys (name, description, etc.)
- Findings tools use namespaced filter keys (occurrence:, context:, special:, triage:, type:)
- Tests and Bug Tracking tools use bare filter keys

---

## 2026-02-26 03:40 UTC - S04-1

- Implemented filter validation utility in src/mcp/filter-validation.ts
- Created FILTER_KEYS registry with mappings for all specified resource types:
  - Portfolio: applications, projects, branches (both scoped and org-wide), labels
  - Findings: issues, occurrences, component_versions, component_origins, taxonomies
  - Tests: scan history
  - Bug Tracking: configurations, external_projects, project_mappings
- Implemented extractFilterKeys() function using regex pattern from spec
- Implemented validateFilter() function with advisory validation (returns null for unknown resource types)
- Files changed: src/mcp/filter-validation.ts (new), prp.json (updated S04-1 passes: true)

**Learnings for future iterations:**
- The Ralph parallel agent environment lacks Deno installation and internet access, preventing verification command execution
- Code was implemented following spec requirements and existing TypeScript patterns from src/mcp/types.ts
- FILTER_KEYS includes both exact keys from tool descriptions and inferred keys from API documentation
- Namespaced keys use colons (occurrence:severity) while some use hyphens (component-version:security-risk)
- Filter validation is designed to be non-blocking for forward compatibility

---

## 2026-02-26 03:50 UTC - S04-2

- Enhanced Portfolio tool filter descriptions with valid filter keys
- Added name convenience parameter to get_applications and get_projects
  - Auto-generates RSQL filter: name=='value'
  - Combines with existing filter param using ; (AND operator)
- Updated filter descriptions for all Portfolio tools:
  - get_applications: name, description, subscriptionType, inTrash
  - get_projects: name, description, subItemType, inTrash
  - get_branches: name, isDefault
  - get_portfolio_branches: name, isDefault, projectId, applicationId
  - get_labels: name
- Integrated filter validation in all Portfolio tool handlers using validateFilter()
- Files changed:
  - src/mcp/tools/get-applications.ts (added name param, filter validation, enhanced docs)
  - src/mcp/tools/get-projects.ts (added name param, filter validation, enhanced docs)
  - src/mcp/tools/get-branches.ts (filter validation, enhanced docs)
  - src/mcp/tools/get-portfolio-branches.ts (filter validation, enhanced docs)
  - src/mcp/tools/get-labels.ts (filter validation, enhanced docs)
  - prp.json (updated S04-2 passes: true)

**Learnings for future iterations:**
- Name convenience parameters follow pattern: if present, build `name=='${name}'`, then combine with existing filter via `;` (AND)
- Filter validation uses resource type keys from FILTER_KEYS registry (e.g., "portfolio.applications", "portfolio.projects")
- get_portfolio_branches uses "portfolio.branches.org-wide" resource type (has projectId/applicationId keys)
- get_branches uses "portfolio.branches" resource type (no projectId/applicationId keys)
- All Portfolio tools now import both errorResponse and validateFilter for consistent error handling

---
